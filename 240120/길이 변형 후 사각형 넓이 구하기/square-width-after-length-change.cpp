#include <iostream>

using namespace std;

int main() {
    // 여기에 코드를 작성해주세요.
    int a, b;
    cin >> a >> b;
    int w, h;
    w = a + 8;
    h = b * 3;
    cout << w << endl;
    cout << h << endl;
    cout << w * h;
    return 0;
}