#include <iostream>

using namespace std;

int main() {
    // 여기에 코드를 작성해주세요.
    int y, m, d;
    char a, n;
    n = '-';
    cin >> y >> a >> m >> a >> d;
    cout << m << n << d << n << y;
    return 0;
}